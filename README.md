# TaskRover
An enterprise-grade field operations portal built with React and Django. Features include strict role-based access, task assignments, team management, and a custom Mock AI service for field report verification.
